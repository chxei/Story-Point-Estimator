---
description: Serve the application locally for testing
---

Use this workflow to start a local web server and test the Story Point Estimator in your browser.

// turbo-all

1. Start a local HTTP server on port 8000:
```bash
python3 -m http.server 8000
```
2. Once the server is running, the application will be available at `http://localhost:8000`.
